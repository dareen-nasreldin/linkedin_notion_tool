# Install with: python install_deps.py
# (jobspy pins numpy==1.26.3 which has no Python 3.14 wheel,
#  so we install numpy 2.x first then jobspy --no-deps)
notion-client
python-dotenv
pandas
playwright
requests
tls-client
pydantic
regex
beautifulsoup4
markdownify
